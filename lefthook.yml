pre-commit:
  parallel: true
  commands:
    tsc:
      run: pnpm tsc
    lint:
      glob:
        - "*.{js,ts,jsx,tsx,json,css}"
        - "!env.d.ts"
      run: pnpm biome check --fix --no-errors-on-unmatched {staged_files}
      stage_fixed: true
